# Backend env example
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/campusbooks?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=your_jwt_secret_here
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
FIREBASE_PROJECT_ID=your_firebase_project_id

# Firebase Admin: put the service account JSON here (use JSON string or base64-encoded)
# Example (JSON string):
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...",...}

# Or set the path to a service account file:
# FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/serviceAccountKey.json

# Allowed email domain(s) (comma-separated) — users must sign in with college emails
ALLOWED_EMAIL_DOMAIN=@yourcollege.ac.in
